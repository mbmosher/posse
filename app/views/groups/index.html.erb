<h1>My Posses</h1>

<p>
<%= link_to 'Back', welcome_index_path, class: "btn"  %> <%= link_to "New Posse", new_group_path, class: 'btn' %> <%= link_to "All Posse Events", allevents_path, class: 'btn' %>
</p>

<table class="table table-striped">
  <tr>
    <th>Posse</th>
    <th>Description</th>
  </tr>
 
  <% @groups.each do |group| %>
    <tr>
      <td><%= link_to group.name, group_path(group) %></td>
      <td><%= group.description %></td>
    </tr>
  <% end %>
</table>

